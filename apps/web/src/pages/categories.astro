---
/**
 * Categories Page - Migrada a AppLayout
 */
import AppLayout from '../layouts/AppLayout.astro';
import { CategoryManager } from '../components/CategoryManager';
import { PageHeader } from '../components/ui/PageHeader';
import { TipsCard, HowItWorksCard } from '../components/ui/SidebarCard';
import { QuickActions } from '../components/ui/QuickActions';
---

<AppLayout 
  title="Categorías - Hybrid Brain 🧠" 
  description="Gestiona tus categorías de conocimiento"
>
  <!-- Page Header -->
  <Fragment slot="header">
    <PageHeader
      title="Categorías"
      subtitle="Organiza tu conocimiento en categorías jerárquicas para mejorar la búsqueda."
      icon="📂"
      client:load
    />
  </Fragment>

  <!-- Main Content -->
  <Fragment slot="main">
    <CategoryManager client:load />
  </Fragment>

  <!-- Sidebar -->
  <Fragment slot="sidebar">
    <HowItWorksCard 
      title="🎯 Uso de categorías"
      steps={[
        "Crea categorías padre (ej: Programación)",
        "Añade subcategorías (ej: JavaScript)",
        "Asigna contenido al procesar",
        "Filtra en el chat por categoría"
      ]}
      client:load
    />

    <TipsCard 
      tips={[
        "Las categorías son jerárquicas (padre → hijo)",
        "Al buscar en una categoría, se incluyen subcategorías",
        "Arrastra una categoría sobre otra para reorganizar",
        "Sincroniza con las propiedades de Notion"
      ]}
      client:load
    />

    <QuickActions
      actions={[
        { icon: "💬", label: "Chat con tu cerebro", href: "/chat" },
        { icon: "➕", label: "Procesar contenido", href: "/dashboard" },
        { icon: "🔮", label: "Motor semántico", href: "/indexing" }
      ]}
      client:load
    />
  </Fragment>
</AppLayout>
