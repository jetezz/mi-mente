---
/**
 * Dashboard Page - Migrada a AppLayout
 */
import AppLayout from '../layouts/AppLayout.astro';
import { EnhancedDashboard } from '../components/EnhancedDashboard';
import { PageHeader } from '../components/ui/PageHeader';
import { TipsCard, HowItWorksCard } from '../components/ui/SidebarCard';
import { QuickActions } from '../components/ui/QuickActions';
---

<AppLayout 
  title="Dashboard - Hybrid Brain 🧠" 
  description="Procesa y organiza contenido multimedia con IA"
>
  <!-- Page Header -->
  <Fragment slot="header">
    <PageHeader
      title="Dashboard"
      subtitle="Procesa videos de YouTube e Instagram y guárdalos en tu segundo cerebro."
      icon="📥"
      client:load
    />
  </Fragment>

  <!-- Main Content -->
  <Fragment slot="main">
    <EnhancedDashboard client:load />
  </Fragment>

  <!-- Sidebar -->
  <Fragment slot="sidebar">
    <TipsCard 
      tips={[
        "Los videos más largos tardan más en procesarse",
        "La transcripción funciona mejor en español e inglés",
        "Configura tu Notion para organizar mejor las notas",
        "Usa el prompt personalizado para instrucciones específicas"
      ]}
      client:load
    />

    <QuickActions
      actions={[
        { icon: "💬", label: "Chat con tu cerebro", href: "/chat" },
        { icon: "🔮", label: "Indexación semántica", href: "/indexing" },
        { icon: "📂", label: "Gestionar categorías", href: "/categories" }
      ]}
      client:load
    />
  </Fragment>
</AppLayout>
